// Lab_03_4.cpp
// Бахмат Катерина
// Лабораторна робота № 3.4
// Розгалуження, задане плоскою фігурою.
// Варіант 1

#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	double x; 
	double y; 
	double R;

	cout << "x = "; cin >> x;
	cout << "y = "; cin >> y;

	if (y = 0, (x* x) + (y * y) < R * R, y > x || y < x, (x * x) + (y * y) > R, y = 0)
		cout << "yes" << endl;
	else
		cout << "no" << endl;



	cin.get();
	return 0;
}
